<tr data-id="<%= store.id %>">
  <%= data_td_list store,
                   :code,
                   [up_level(store.region_id), collection: regions_hash ],
                   [:region_id, collection: regions_hash ],
                   :name,
                   :area,
                   "", # :depot_area
                   :location,
                   "" # :grade
  %>
</tr>
