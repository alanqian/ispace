<h1>Listing import_sheets</h1>

<p>should show:<br>
<p>  import_date, comment, filename, imported: count..., #show, #discard link

<p>product: category:*, product:* + brand:*, supplier:*, manufacture:*
<p>sale: sale:*
<p>test: all fields

<table>
  <thead>
    <tr>
      <th>Store</th>
      <th>User</th>
      <th>Filename</th>
      <th>Comment</th>
      <th>Type</th>
      <th>Mapping</th>
      <th>Imported</th>
      <th>_do</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @import_sheets.each do |import_sheet| %>
      <tr>
        <td><%= import_sheet.store_id %></td>
        <td><%= import_sheet.user_id %></td>
        <td><%= import_sheet.filename %></td>
        <td><%= import_sheet.comment %></td>
        <td><%= import_sheet.type %></td>
        <td><%= import_sheet.mapping %></td>
        <td><%= import_sheet.imported %></td>
        <td><%= import_sheet._do %></td>
        <td><%= link_to 'Show', import_sheet_path(import_sheet, part: "result") %></td>
        <td><%= link_to 'Destroy', import_sheet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Import sheet', new_import_sheet_path(_t: @t) %>
