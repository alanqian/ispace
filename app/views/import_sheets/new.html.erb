<% content_for_title "new_#{@import_sheet._target}" %>

<!--
<h2>数据导入: <%= t("activerecord.models.#{}") %> </h2>
-->

<div>
<%= render 'form', import_sheet: @import_sheet %>
</div>

<% if @t == 'sale' or @t == 'product' %>
  <%= link_to '查看导入历史', import_sheets_path(_t: @t) %>
<% end %>
